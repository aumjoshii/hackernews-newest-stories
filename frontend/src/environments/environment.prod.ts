export const environment = {
  production: true,
  // When deployed alongside the API in Azure App Service this can be a relative path,
  // or override at build time via fileReplacements in angular.json.
  apiBaseUrl: '/api'
};
